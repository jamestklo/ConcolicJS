\section{Approach}

\header{Indexing Functions.}
% gives AST ID rather than function names, because it's common for functions to share the same name, or more commonly, to be anyonmous or have no name at all  (function () {});

\header{Decorated Execution.}
solves complex conditions
to solve precedence, we have 

(a && a.b)
_AND(a, _GET(a,b)); 
_AND((_t=a, _t), _t && _GET(_t, b));

result = f0(input) || f1(input);
_OR(

\header{Backward Slicing.}
% JavaScript API


\header{DOM Solver.}


\header{Sync XHR for eval().}
% code inlined within HTML

